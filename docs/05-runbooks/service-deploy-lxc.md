# 服务部署通用流程（LobeChat 等）

## 目标
把 LXC 当成“最小可维护单元”：有快照、可回滚、有备份。

## 步骤（高层）
1. 创建 LXC（记录：CPU/MEM/Disk/网段/IP）
2. 初始化基线：时区、更新源、SSH key、基础工具
3. 部署服务（systemd 或 docker）
4. 反代接入（域名/TLS/访问控制）
5. 做首次快照（命名：pre-<service>-v1）
6. 备份策略纳入（配置 + 数据）

## 验收标准
- 服务在重启后自启动
- 有健康检查/可观测入口（至少日志）
- 有回滚快照
